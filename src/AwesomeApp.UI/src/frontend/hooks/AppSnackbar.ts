/**
 * @exports useAppSpinner Hook that delivers Snackbar API inside app Views. 
 * The purpose of alias is to confirm that Snackbar context has been used in Views root and can be safely consumed in its children 
 */
export { useSnackbar as useAppSnackbar } from '../components/snackbar'
