/**
 * @exports useAppSpinner Hook that delivers Spinner API inside app Views. The purpose of alias is to confirm that Spinner context has been used in Views root and can be safely consumed in its children 
 */
export { useSpinner as useAppSpinner } from '../components/spinner'
